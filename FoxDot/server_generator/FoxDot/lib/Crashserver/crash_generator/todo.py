'''
TODO :

add weight to fx, synth, sample
attr synth / dictionnaire de param synth 

add kick event
#add lpf / hpf to each player
#linvar fx
max degree en midi
#sus loop ( position ?)
jump player para à revoir

Master().hpf=var([0, linvar([400, 3200],16)], [24, 8])

stop les player crash quand server reviens en toggle


#exposer parametres globaux
ajouter des loops
more synth

#add minimum run player time

upgrade future -> change to oop 

#synth à revoir
glitchbass oct (<6)?
scatter 
virus aigu.
grate
gray

#fx à revoir 
ringz = 9

# log error
<mt - fbass>.dur=(PChain(krhytm)[:16]*mt.sus).limit(sum,16).limit(len,20)
int() argument must be a string, a bytes-like object or a number, not 'PGroup'

loop
'NoneType' object has no attribute 'keys'

'''